use: "pip install -r requirements.txt" in the terminal to install all the pre-requisites


NOTE: Gmail doesn't allow the usage of account password to login to third party applications so we need to make an application password through Google's 2-Step Verification Process. 